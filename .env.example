# Phase 0 placeholders. Do not commit real credentials.
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/perflo_ap
COMPOSIO_API_KEY=
OPENAI_API_KEY=...
CLASSIFIER_MODEL=gpt-5-mini
# "perflo" alone only works if installed globally. Confirmed working without
# that: the npx form below.
PERFLO_CLI_PATH=npx @perflo/cli@latest
APP_URL=http://localhost:3000
# FR-1 default is 300 (5 min). Lower for testing to watch mail arrive live.
POLL_INTERVAL_SECONDS=300
